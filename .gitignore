.env
.env.local
.vercel
node_modules/
.DS_Store
__pycache__/
*.pyc

# Claude context — local only, do not publish
CLAUDE.md
HANDOFF.md
.kb.local.json

# Playwright browsers (downloaded binaries, not source)
scripts/screenshots/
.pw-browsers/
# also match a symlink pointing at a shared browser cache
.pw-browsers
.env*

# Local scratch/debug artifacts (never committed)
_*.json
_*.mjs
_*.py
# The capture runbook writes the RAW Classroom snapshot here. Only the redactor
# may produce anything committable from it, so the whole directory stays out.
scratchpad/

# Out-of-band gate results (machine-local, never committed)
.gate/

# Loop/build noise in the agent workspace (never committed)
server.log
.playwright/
